/*
import Vue from 'vue';
import App from './App.vue';

Vue.config.productionTip = false;


иницилизируется vue inctance - vue экземпляр
и крепится к нашему диву с id #app
в функции render указывается компонент приложения App
js module - это файл который экспортирует определенный набор данных, который мы укажем, это могут быть классы строки массивы
import записывается через ключ import и название переменной в которую мы запишем эти данные from  и далее из какого файла мы получим эти данные
new Vue({
  render: (h) => h(App),
}).$mount('#app');

*/
